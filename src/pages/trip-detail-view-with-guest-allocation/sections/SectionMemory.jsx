import React from 'react';
import SectionCard, { PlaceholderNote } from './_SectionCard';

export default function SectionMemory() {
  return (
    <SectionCard accent="accent" lead="" italic="Memory.">
      <PlaceholderNote>Memory content coming in a later phase.</PlaceholderNote>
    </SectionCard>
  );
}
