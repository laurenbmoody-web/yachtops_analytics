import React from 'react';
import SectionCard, { PlaceholderNote } from './_SectionCard';

export default function SectionCrew() {
  return (
    <SectionCard accent="brass" lead="Crew on " italic="this trip.">
      <PlaceholderNote>Crew content coming in a later phase.</PlaceholderNote>
    </SectionCard>
  );
}
