import React from 'react';
import SectionCard, { PlaceholderNote } from './_SectionCard';

export default function SectionProvisioning() {
  return (
    <SectionCard accent="navy" lead="" italic="Provisioning.">
      <PlaceholderNote>Provisioning content coming in a later phase.</PlaceholderNote>
    </SectionCard>
  );
}
